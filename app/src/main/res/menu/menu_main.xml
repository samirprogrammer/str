<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/menu_search"
        android:icon="@drawable/ic_search_black_24dp"
        android:title=""
        app:actionViewClass="android.widget.SearchView"
        app:showAsAction="ifRoom|collapseActionView" />
    <item
        android:id="@+id/action_settings"
        android:icon="@drawable/ic_apps_white_24dp"
        android:orderInCategory="100"
        android:title="@string/action_settings"
        app:showAsAction="always">
        <menu>
            <item
                android:id="@+id/action_no_ads"
                android:icon="@drawable/ic_no_ads" 
                android:title="Remove Ads" />
            <item
                android:id="@+id/menu_famous_apps"
                android:icon="@drawable/dialer" android:visible="false"
                android:title="Our Apps" />
            <item
                android:id="@+id/menu_theme"
                android:icon="@drawable/theme"
                android:title="Dark Theme" />

            <item
                android:id="@+id/action_rate"
                android:icon="@drawable/ic_rate_us"
                android:title="Rate Us!" />
            <item
                android:id="@+id/action_feedback"
                android:icon="@drawable/ic_feedback"
                android:title="Submit issue/feeback" />

        </menu>
    </item>

</menu>